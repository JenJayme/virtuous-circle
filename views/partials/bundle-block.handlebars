<li class="listItem">
    {{quantity}} of {{bundle_name}}

    <button class="waves-effect waves-light btn claimItem" data-id="{{id}}" data-claimed="{{claimed}}">
        {{#unless claimed}}Claim{{/unless}}
    </button>

</li>