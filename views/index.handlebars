<div class="row">
    <div class="col s6">
        <h3>Offered</h3>
        <ul class="offered">
            {{#each foodbundles}}
            {{partials/bundle-block}}
            {{/each}}
        </ul>
    </div>

    <div class="col s6">
        <h3>Claimed</h3>

        <ul class="claimed">
            {{#each foodbundles}}
            {{/each}}
        </ul>
    </div>

    <div class="row">
        <div class="col s4"></div>
        <div class="col s4">

            <h2>Add A Bundle</h2>
            <form class="create-form">
                <div class="form-group">
                    <label for="offered">What kind of food supplies would you like to offer?</label><br>

                    <input type="text" name="bundleName" placeholder="What is it?"></div>
                <input type="text" name="quantity" placeholder="How much or how many, in boxes, cans, cases?"><br>

                <button class="btn waves-effect waves-light" type="submit" name="action">submit
                    <i class="material-icons right">send</i>
                </button>
                <div class="col s4"></div>
            </form>
        </div>
    </div>
</div>